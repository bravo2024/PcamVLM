# Pinned lower bounds — match Google's MedGemma recipe + current PaliGemma2 fix
torch>=2.3.0
transformers>=4.50.0     # Gemma 3 support landed in 4.50
accelerate>=1.0.0
peft>=0.13.0             # LoraConfig + prepare_model_for_kbit_training
bitsandbytes>=0.45.0     # nf4 + double_quant
datasets>=3.5.0
pillow>=10.0.0
numpy>=1.26.0
pandas>=2.0.0
tqdm>=4.66.0

# Training
trl>=0.12.0              # SFTTrainer + SFTConfig with bf16, packing, etc.

# Inference helpers
qwen-vl-utils>=0.0.10    # process_vision_info (not strictly required for Gemma3
                          # but kept for cross-VLM compatibility)

# App
streamlit>=1.36.0
matplotlib>=3.8.0

# Tests
pytest>=8.0.0
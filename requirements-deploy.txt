# Lightweight deps for Streamlit Cloud deployment (no GPU, no transformers)
# Install: pip install -r requirements-deploy.txt
torch>=2.3.0
torchvision>=0.18.0
pillow>=10.0.0
numpy>=1.26.0
pandas>=2.0.0
streamlit>=1.36.0
matplotlib>=3.8.0

# Tests
pytest>=8.0.0
